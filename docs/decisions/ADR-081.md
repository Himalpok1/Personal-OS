# ADR-081 — Checkpoint 10.9: the Agent Gateway — a versioned read-tool contract bound for the first time, an agent identity with trust levels, per-principal read permissions, budgeted and audited tool calls, agent proposals through the Action Framework, and Guard 8 (migration 0025)

**Status:** Locked

**Checkpoint 10.9 (2026-09-18, owner-directed; four scope decisions owner-approved the same
day).** The brief moves Personal OS from "can safely prepare and execute controlled actions"
(ADR-078) to "a future agent can operate through Personal OS without bypassing its privacy,
permission, memory, context, action and audit boundaries" — and forbids selecting or integrating
any agent runtime (OpenClaw, Hermes, LangGraph or other), any autonomous loop, planning engine,
vector store, knowledge graph, LLM memory or generic tool registry. The guiding principle:
*Personal OS owns identity, context, memory, permissions, tools, actions, approvals and
auditability; the agent is replaceable.* A three-lane read-only discovery preceded any code; its
findings are §1. The four decisions the discovery could not make alone were put to the owner and
decided: a migration (§3); device-bound approval now (ADR-082); the five ADR-066 tools **plus**
`get_academic_context` under its own OFF-by-default permission (ADR-070b); an Agent Center on
mobile with the existing approval sheet, no chatbot (§9).

**1. WHAT THE DISCOVERY ESTABLISHED.** (i) The API had no principal abstraction: device-token auth
lived only on `/devices/*` (ADR-029) and `principal` was the literal `"app"` at eight sites in
`apps/api/src/actions/service.ts`; nothing recorded a *read*. Identity and read-audit therefore
need a migration; the write side (`action_requests`, `permission_grants`, the `agent` CHECK
member) does not. (ii) Every action/permission wire shape the deployed versionCode-33 client
parses is `.strict()`, so agent attribution cannot be a new key (the 10.2 lesson) — it rides on
the existing `principal: "agent"` and `source_ref`, plus a separate agent read model. (iii) Guards
4–7 walk literal directory lists; a new `apps/api/src/agent/` directory was invisible to all of
them. (iv) Every owner route being Tailscale-perimeter-only meant a tailnet process could call
`POST /actions/:id/approve` itself — the confused deputy ADR-078 §5 deferred to "the agent ADR".
(v) `buildTodayContext` requires a `WeakSet`-branded grant minted only inside a live request by
`authorizeCloudAsk`, which needs the `ask` consent row; Guard 3 forbids every forging cast.
(vi) `READ_TOOL_MAX_CALLS_PER_REQUEST` / `READ_TOOL_MAX_CHARS_PER_REQUEST` had zero enforcement
sites; `get_calendar_context` / `get_task_context` had input schemas only.

**2. THE ARCHITECTURE: A BOUNDARY, NOT A LANE.** *(Lane D fills: the contract diagram; owner
routes `/agents/*` device-bound vs agent routes `/agent/*` agent-token-bound; no agent route can
approve; reads are tools, writes are actions; the gateway returns data to a registered principal,
never to a model; Guard 1's set unchanged.)*

**3. IDENTITY (MIGRATION 0025, LEVEL 25 → 26).** *(Lane D fills: `agents` — name ≤ 60, CHECKed
`trust_level none|read|propose`, `token_hash` UNIQUE (sha256 of a `posa_`-prefixed 256-bit token
returned once), `disclosure_version`, `revoked_at` never DELETE, `last_seen_at` diagnostic;
`agent_tool_calls` — one row per call incl. refusals, no payload, CHECKed tool name/status/
error-pair, 30-day retention; `action_requests.agent_id` FK + `correlation_id` + the
`principal='agent' ⟺ agent_id` pair CHECK + `source` widened to `agent` (the one deliberate change
to a frozen enum, and why it is safe); every column justified; neither agent table in
`GET /export`; image-only rollback.)*

**4. TRUST LEVELS AND PER-PRINCIPAL PERMISSIONS.** *(Lane D fills: `none|read|propose`; no
`operator` member — ADR-079 §4's trusted operator is host access outside the product; grants stay
per principal (`agent`) with trust as the per-agent axis; `READ_PERMISSIONS = context.read |
items.read | academic.read` as a SIBLING of the wire-frozen `ACTION_PERMISSIONS`; agent grants
ship OFF; `memory.read` reserved and parse-fail tested; never health, mail, credentials, AI
config; `runHandler` re-checks under the row's own principal; `setPermissionGrant`'s
pending-cancel principal-scoped.)*

**5. THE READ-TOOL CONTRACT, BOUND.** *(Lane D fills: six tools; `READ_TOOL_OUTPUT_SCHEMAS`
strict and body-free except `get_item_context`; ids carried; the second grant-mint site
`authorizeAgentRead` sharing one WeakSet and one `assertGrant`, no cast; `buildTodayContext`
unchanged; calendar/task builders under Guard 4's allowlist; budgets enforced per
`correlation_id` (≤ 6 calls, ≤ 30 000 chars) plus 60/min per agent; the manifest via
`z.toJSONSchema`, `validation: "server-side"`, `contract_version`.)*

**6. PROPOSALS THROUGH THE ACTION FRAMEWORK — AND AN AMENDMENT TO ADR-078 §6.** *(Lane D fills:
`POST /agent/actions` → `createActionRequest` with attribution; `source='agent'`, `source_ref =
agent.id`; `reason` REQUIRED, control-stripped, bounded, agent-authored UNTRUSTED display text
rendered quoted under "Agent says" and never as the Why line; no approve route; own-rows-only
list/cancel with 404 for a foreign id; `client_uuid` idempotency reused.)*

**7. AUDIT, RETENTION, EXPORT, LOGGING.** *(Lane D fills.)*

**8. GUARD 8.** *(Lane D fills: (a)–(d) as implemented in `ai-egress-guard.test.ts`; the `agent`
member of the "an AI lane" alternation; the retention-job exemption.)*

**9. MOBILE.** *(Lane D fills: Agent Center at `/agents`, `/agents/new` (token shown once,
disclosure byte-pinned), `/agents/[id]` (trust, permissions, revoke, correlation-grouped
activity); Settings card after Actions; the approval sheet's agent treatment; Today untouched;
the concepts evaluated in `docs/AGENT-UX.md`.)*

**10. WHAT IS STRUCTURALLY ABSENT.** *(Lane D fills: no runtime, no loop, no session store, no
agent approve, no agent grant, no memory tool, no health/mail tool, no model call, no pg-boss.)*

**11. ROLLBACK AND LIFECYCLE.** *(Lane D fills.)*

**Unchanged and reaffirmed:** *(Lane D fills.)*
