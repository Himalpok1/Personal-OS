{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file=../../.env dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "pairing:generate": "tsx --env-file=../../.env src/scripts/generate-pairing-code.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/multipart": "^10.1.1",
    "@personal-os/ai-providers": "workspace:*",
    "@personal-os/calendar-providers": "workspace:*",
    "@personal-os/core": "workspace:*",
    "@personal-os/db": "workspace:*",
    "@personal-os/health-providers": "workspace:*",
    "@personal-os/schema": "workspace:*",
    "ai": "^7.0.66",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.12.0",
    "pg-boss": "^12.27.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.23.12",
    "vitest": "^4.1.10"
  }
}
